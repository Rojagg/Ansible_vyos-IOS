Conclusion:i

THe interface configuration should use the module designed for this speciofic purpose 
it makes our code more scalable, but using the dedicated-prupose module is not always the best choice,
for example the router ospf 1 command is always sent to the cisco ios devices - so alwys output changed = 1
